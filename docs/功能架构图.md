# 网络五子棋功能架构图

本文档用 Mermaid 描述项目的功能分层、联机匹配流程和人机对战流程。Mermaid
代码可以在支持 Mermaid 的 Markdown 预览器中直接渲染，也可以复制到
Mermaid Live Editor 中查看。

## 一、系统功能总览

```mermaid
flowchart TB
    USER["用户"]

    subgraph CLIENT["客户端进程"]
        UI["ui 界面层<br/>GameFrame / BoardPanel / BoardTheme / ReplayDialog"]
        PLAYER["player 玩家层<br/>Player / HumanPlayer / ComputerPlayer"]
        AI["ai 算法层<br/>GomokuAI"]
        NET["network 网络客户端层<br/>OnlineClient / NetworkListener / NetworkMessage"]
        REPLAY["replay 回放层<br/>ReplayFileService"]
    end

    subgraph SERVER["服务器进程"]
        SRV["server 服务层<br/>GomokuServer / ClientHandler"]
        LOBBY["在线大厅与匹配<br/>在线用户 / 随机队列 / 指定邀请 / 60 秒超时"]
        ROOM["GameRoom 房间与裁判<br/>权威棋盘状态 / 落子 / 聊天 / 悔棋 / 重开"]
    end

    subgraph COMMON["公共业务层"]
        CORE["core 规则层<br/>Board / GameSession / MoveResult"]
        MODEL["model 数据模型层<br/>Stone / Point / Move / ChatMessage / GameMode"]
    end

    subgraph DATA["数据持久化"]
        DB["database 数据访问层<br/>DatabaseInitializer / AccountRepository / GameRepository"]
        MYSQL[("MySQL 8.x<br/>账号 / 对局 / 落子 / 聊天")]
        FILE["回放文件<br/>.gomoku"]
    end

    USER --> UI

    UI --> CORE
    UI --> PLAYER
    UI --> NET
    UI --> REPLAY

    PLAYER --> AI
    PLAYER --> CORE
    AI --> CORE
    NET --> MODEL
    REPLAY --> MODEL
    CORE --> MODEL

    NET <-->|"TCP Socket / 文本协议"| SRV
    SRV --> LOBBY
    SRV --> ROOM
    ROOM --> CORE
    ROOM --> MODEL
    SRV --> DB
    DB --> MYSQL
    REPLAY --> FILE
```

## 二、联机注册、登录与匹配流程

```mermaid
sequenceDiagram
    actor U as 用户
    participant GUI as GameFrame
    participant CLIENT as OnlineClient
    participant SERVER as GomokuServer
    participant HANDLER as ClientHandler
    participant ACCOUNT as AccountRepository
    participant ROOM as GameRoom
    participant DB as MySQL

    U->>GUI: 输入用户名和密码
    GUI->>CLIENT: register(username, password)
    CLIENT->>SERVER: REGISTER
    SERVER->>HANDLER: 分配给客户端独立处理线程
    HANDLER->>ACCOUNT: 创建账号
    ACCOUNT->>DB: player_account
    DB-->>ACCOUNT: 注册结果
    ACCOUNT-->>HANDLER: 成功或失败
    HANDLER-->>CLIENT: REGISTER_RESULT
    CLIENT-->>GUI: 显示注册结果

    U->>GUI: 点击登录
    GUI->>CLIENT: login(username, password)
    CLIENT->>SERVER: LOGIN
    HANDLER->>ACCOUNT: 校验密码摘要和盐值
    ACCOUNT->>DB: 查询账号
    DB-->>ACCOUNT: 账号数据
    ACCOUNT-->>HANDLER: 登录结果
    HANDLER-->>CLIENT: LOGIN_RESULT
    HANDLER-->>CLIENT: ONLINE_USERS
    CLIENT-->>GUI: 进入在线大厅

    alt 随机匹配
        GUI->>CLIENT: requestRandomMatch()
        CLIENT->>SERVER: MATCH_RANDOM
        SERVER->>SERVER: 加入随机匹配队列
        SERVER-->>CLIENT: MATCH_WAITING（60 秒）
        SERVER->>SERVER: 找到另一名等待玩家
    else 指定用户名匹配
        GUI->>CLIENT: requestTargetMatch(target)
        CLIENT->>SERVER: MATCH_TARGET
        SERVER-->>CLIENT: MATCH_WAITING（60 秒）
        SERVER-->>HANDLER: MATCH_INVITE
        HANDLER-->>CLIENT: 对方接受邀请
    end

    SERVER->>ROOM: 创建房间并加入两名玩家
    ROOM-->>CLIENT: START（黑方、白方、执子颜色）
    CLIENT-->>GUI: 进入联机对局界面
```

## 三、联机对局数据流

```mermaid
flowchart LR
    A["玩家点击棋盘交叉点"] --> B["BoardPanel 转换行列坐标"]
    B --> C["GameFrame 判断是否轮到自己"]
    C --> D["OnlineClient.sendMove(row, col)"]
    D -->|"MOVE"| E["ClientHandler 接收消息"]
    E --> F["GameRoom 校验回合和落点"]
    F --> G["GameSession.placeMove"]
    G --> H{"四方向是否五连？"}
    H -- "否" --> I["广播 MOVE 和下一手棋子"]
    H -- "是" --> J["广播 GAME_END"]
    I --> K["双方客户端重绘棋盘"]
    J --> L["显示胜负并结束当前对局"]
    J --> M["后台线程保存 MySQL 对局记录"]
```

服务器是联机对局的权威状态来源。客户端只发送请求，不能直接决定棋局结果；
`GameRoom` 校验回合、坐标和当前房间状态后，再由 `GameSession` 更新棋盘并广播。

## 四、人机对战流程

```mermaid
flowchart TB
    A["玩家执黑点击棋盘"] --> B["GameFrame.handleBoardMove"]
    B --> C["GameSession.placeMove"]
    C --> D{"对局是否结束？"}
    D -- "是" --> E["显示胜负结果"]
    D -- "否，轮到电脑" --> F["aiExecutor 后台线程"]
    F --> G["ComputerPlayer.decideMove"]
    G --> H["GomokuAI.findBestMove"]
    H --> I["中心开局或已有棋子附近候选点"]
    I --> J["优先寻找自己的立即获胜点"]
    J --> K["其次阻挡对手的立即获胜点"]
    K --> L["棋型评分与位置评分，选择最高分"]
    L --> M["SwingUtilities.invokeLater"]
    M --> N["GameSession.placeMove 电脑落子"]
    N --> O["BoardPanel 重绘"]
```

`GomokuAI` 使用评分表法，不进行深度极大的极小极大搜索。它先把四个方向上的
局部棋型转换为字符串，再根据五连、活四、冲四、活三等棋型设置分值。人机计算
放在后台线程中，结果回到 Swing EDT 后再更新棋盘，避免界面卡顿。

## 五、功能与技术对应关系

| 功能 | 主要实现位置 | 说明 |
| --- | --- | --- |
| 15×15 棋盘和鼠标落子 | `ui.BoardPanel`、`ui.GameFrame` | Java2D 绘制棋盘和棋子，鼠标坐标转换为行列 |
| 胜负判断 | `core.Board`、`core.GameSession` | 横向、纵向、正斜、反斜四方向查找连续五子 |
| 本地双人 | `ui.GameFrame`、`core.GameSession` | 同一进程内轮流落子 |
| 人机对战 | `player.ComputerPlayer`、`ai.GomokuAI` | 后台线程调用评分表 AI |
| 注册和登录 | `database.AccountRepository`、`server.ClientHandler` | 密码使用盐值和摘要保存 |
| 在线大厅 | `server.GomokuServer`、`network.OnlineClient` | 维护在线用户列表并实时广播 |
| 随机匹配 | `GomokuServer.requestRandomMatch` | 等待队列配对，超时时间 60 秒 |
| 指定用户匹配 | `GomokuServer.requestTargetMatch` | 按用户名发送邀请，对方同意后开局 |
| 联机落子 | `server.GameRoom`、`network.OnlineClient` | 服务器权威校验并广播结果 |
| 聊天 | `GameRoom.handleChat`、`OnlineClient.dispatch` | 房间内消息实时转发 |
| 悔棋 | `GameRoom`、`core.GameSession.undoLastMove` | 对方同意后回退一步 |
| 重新开始 | `GameRoom.handleRestartRequest` | 双方确认后重置房间棋局 |
| 游戏回放 | `replay.ReplayFileService`、`ui.ReplayDialog` | 保存 `.gomoku` 文件并逐步播放 |
| MySQL 初始化 | `database.DatabaseInitializer` | 启动时执行 `sql/init.sql` |
| 对局存档 | `database.GameRepository` | 保存对局、落子和聊天记录 |
| 棋盘换肤 | `ui.BoardTheme`、`ui.BoardPanel` | 切换原木、青玉、海蓝主题 |
| 清屏动画 | `ui.BoardPanel`、`ui.GameFrame` | 使用 Swing 计时器播放扫光效果 |

## 六、线程模型

| 线程或线程池 | 归属 | 作用 |
| --- | --- | --- |
| Swing EDT | 客户端 | 处理按钮、鼠标、棋盘重绘和弹窗 |
| `aiExecutor` | 客户端 | 后台执行 AI 搜索，避免阻塞界面 |
| `OnlineClient` 读取线程 | 客户端 | 持续读取服务器消息并回调 `NetworkListener` |
| 服务器接受连接线程 | 服务端 | 循环执行 `ServerSocket.accept()` |
| `ClientHandler` 处理线程 | 服务端 | 每个客户端连接一个读消息线程 |
| `gomoku-match-timer` | 服务端 | 处理 60 秒匹配超时和取消 |
| `gomoku-room-save` | 服务端 | 对局结束后异步写入 MySQL |

## 七、包职责简表

| 包名 | 职责 |
| --- | --- |
| `io.github.tissyboxc.gomoku.ui` | Swing 窗口、棋盘绘制、主题、回放窗口和事件处理 |
| `io.github.tissyboxc.gomoku.core` | 15×15 棋盘、落子规则、胜负判断和棋局状态 |
| `io.github.tissyboxc.gomoku.ai` | 评分表法五子棋 AI |
| `io.github.tissyboxc.gomoku.player` | 玩家抽象、人类玩家和电脑玩家 |
| `io.github.tissyboxc.gomoku.network` | TCP 客户端、网络监听接口和消息协议 |
| `io.github.tissyboxc.gomoku.server` | 服务器、客户端处理器、大厅匹配和联机房间 |
| `io.github.tissyboxc.gomoku.database` | JDBC 连接、数据库初始化和数据访问 |
| `io.github.tissyboxc.gomoku.replay` | `.gomoku` 回放文件的保存和读取 |
| `io.github.tissyboxc.gomoku.model` | 棋子、坐标、棋步、聊天和游戏模式模型 |

